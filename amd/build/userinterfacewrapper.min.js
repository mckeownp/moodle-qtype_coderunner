/******************************************************************************
 *
 * This module provides a wrapper for user-interface modules, handling hiding
 * of the textArea that is being replaced by the UI element,
 * resizing of the UI component, and support of such usability functions as
 * ctrl-alt-M to disable/re-enable the entire user interface, including the
 * wrapper.
 *
 * @module coderunner/userinterfacewrapper
 * @copyright  Richard Lobb, 2015, The University of Canterbury
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 *
 * The InterfaceWrapper class is constructed either by Moodle PHP calls of
 * the form
 *
 * $PAGE->requires->js_call_amd($modulename, $functionname, $params)
 *
 * (e.g. from within render.php) or by JavaScript require calls, e.g. from
 * authorform.js when the question author changes UI type.
 *
 * The InterfaceWrapper provides:
 *
 * 1. A constructor InterfaceWrapper(uiname, textareaId) which
 *    hides the given text area, replaces it with a wrapper div (resizable in
 *    height by the user but with width resizing managed by changes in window
 *    width), created an instance of nameInstance as defined in the file
 *    ui_name.js (see below).
 *    params is a record containing the decoded value of
 *
 * 2. A stop() method that destroys the embedded UI and hides the wrapper.
 *
 * 3. A restart() method that shows the wrapper again and re-creates the prior
 *    embedded UI component within it.
 *
 * 4. A loadUi(uiname, params) method that kills any currently running UI element
 *    (if there is one) and (re)loads the specified one. The params parameter
 *    is a record that allows additional parameters to be passed in, such as
 *    those from the question's uiParams field and, in the case of the
 *    Ace UI, the 'lang' (language) that the editor is editing. This data
 *    is supplied by the PHP via the data-params attribute of the answer's
 *    base textarea.
 *
 * 5. Regular checking for any resizing of the wrapper, which are passed on to
 *    the embedded UI element's resize() method.
 *
 * 6. Monitoring of alt-ctrl-M key presses which toggle the visibility of the
 *    wrapper plus UI element and the syncronised textArea by calls to stop()
 *    and restart
 *
 * =========================================================================
 *
 * The embedded user-interface module must be defined in a JavaScript file
 * of the form ui_name.js which must define a class nameInstance with
 * the following functionality:
 *
 * REQUIRED METHODS
 *
 * 1. A constructor SomeUiName(textareaId, width, height, params) that
 *    performs only synchronous, lightweight setup: recording parameters and
 *    reading the textarea's current value. It must NOT attempt to access
 *    window.ace or perform any async work (e.g. template rendering).
 *    textareaId is the ID of the textArea from which the UI element should
 *    obtain its initial serialisation and to which it should write the
 *    serialisation when its sync() or destroy() methods are called. params is
 *    a JavaScript object decoded from the JSON uiParams defined by the question
 *    plus any additional data required, such as 'lang' for the Ace editors.
 *
 * 2. A failed() method that returns true if the constructor detected an error
 *    (e.g. could not de-serialise the textarea's contents). When failed()
 *    returns true the wrapper calls destroy() and aborts UI loading; the
 *    textarea gets the uiloadfailed class and a visible error message.
 *
 * 3. A failMessage() method returning a CodeRunner language-string key
 *    describing the error. Only called when failed() returns true.
 *
 * 4. A getElement() method that returns the HTML element to be inserted into
 *    the document tree. Called only after ready() has resolved, so the element
 *    is guaranteed to exist by then.
 *
 * 5. A sync() method that copies the serialised representation of the UI
 *    plugin's data to the related textarea. Called periodically and on submit.
 *
 * 6. A destroy() method that syncs contents to the textarea then removes any
 *    HTML elements or other created content. Called when Ctrl-Alt-M toggles
 *    the UI off or when the UI is replaced.
 *
 * 7. A resize(width, height) method that resizes the entire UI element to the
 *    given dimensions.
 *
 * 8. A hasFocus() method that returns true if the UI element currently has
 *    keyboard focus.
 *
 * OPTIONAL METHODS
 *
 * 9. A ready() method returning a Promise that resolves when the UI element
 *    returned by getElement() is fully built and ready to be inserted into the
 *    DOM. If absent, the wrapper behaves as if ready() returns Promise.resolve().
 *    Use this for any async initialisation: waiting for window.ace to appear,
 *    rendering Mustache templates, etc. Reject the promise (or throw) to signal
 *    failure; the wrapper will then show the same error UI as a failed()
 *    constructor.
 *
 * 10. A postInsert(wrapperNode) method called immediately after the element
 *    returned by getElement() has been appended to wrapperNode in the live DOM.
 *    Use this for work that requires DOM presence: finding child elements by ID,
 *    creating nested InterfaceWrapper instances (e.g. sub-Ace editors), wiring
 *    event listeners that need real layout. If absent, nothing extra is done.
 *
 * 11. A syncIntervalSecs() method that returns the time interval in seconds
 *    between automatic calls to sync(). Return 0 to disable. The wrapper
 *    provides a default that returns the sync_interval_secs UI parameter when
 *    present, or DEFAULT_SYNC_INTERVAL_SECS otherwise.
 *
 * 12. An allowFullScreen() method that returns true if the UI supports the
 *    full-screen toggle button. Defaults to false if not implemented.
 *
 * 13. A setAllowFullScreen(allow) method (boolean) that overrides the value
 *    returned by allowFullScreen(). Provided so that a parent UI (e.g.
 *    Scratchpad) can control the full-screen behaviour of a child UI.
 *
 * The return value from the module define is a record with a single field
 * 'Constructor' that references the constructor (e.g. Graph, AceWrapper etc)
 *
 *****************************************************************************/
define("qtype_coderunner/userinterfacewrapper",["core/templates","core/notification"],(function(Templates,Notification){function isAnAnswer(textareaId){return/^id_q\d+:\d+_answer$/.test(textareaId)}async function compare_with_last_checked(textareaId){if(!textareaId||!isAnAnswer(textareaId))return null;const textArea=document.getElementById(textareaId),params=textArea.getAttribute("data-params");if(params){const uiParams=JSON.parse(params),lastcheckedanswerhash=uiParams.lastcheckedanswerhash,extractcodefromjson=uiParams.extractcodefromjson;var currentanswer=textArea.value;if(currentanswer=currentanswer.replace(/\r\n/g,"\n"),"1"==extractcodefromjson)try{const answerBits=JSON.parse(currentanswer);"answer_code"in answerBits&&(currentanswer=answerBits.answer_code[0])}catch(error){}const hashHex=function(str){const bytes=(new TextEncoder).encode(str);let hash=2166136261;for(let i=0;i<bytes.length;i++)hash^=bytes[i],hash=16777619*(hash>>>0)>>>0;return hash.toString(16)}(currentanswer);if(lastcheckedanswerhash){const areaToFind="#"+textArea.closest('[id*="question"]').id.replace(/([:\\.#\[\],=])/g,"\\$1")+" .coderunner-test-results";var feedbackArea=document.querySelector(areaToFind);if(feedbackArea){const noticeId=textareaId+"-changed-notice",specificNotice=document.querySelector('[data-id="'.concat(CSS.escape(noticeId),'"]'));if(hashHex!==lastcheckedanswerhash){if(!specificNotice){feedbackArea.classList.add("answer-changed");const message=document.createElement("p");message.textContent="Results below are for a different answer to the answer above.",message.style.color="black",message.style.backgroundColor="greenyellow",message.style.fontSize="larger",message.setAttribute("data-id",noticeId),feedbackArea.parentNode.insertBefore(message,feedbackArea)}}else feedbackArea.classList.remove("answer-changed"),specificNotice&&specificNotice.remove()}}return hashHex}}function InterfaceWrapper(uiname,textareaId){let t=this;this.GUTTER=16,this.DEFAULT_SYNC_INTERVAL_SECS=5,this.uniqueId=Math.random();this.isFullScreenEnable=null,this.taId=textareaId,this.textareaId=textareaId,this.loadFailId=textareaId+"_loadfailerr",this.textArea=document.getElementById(textareaId),this.textArea.current_ui_wrapper&&alert("JavaScript error: multiple UIs on ".concat(textareaId,"!"));const params=this.textArea.getAttribute("data-params");this.uiParams=params?JSON.parse(params):{},this.uiParams.lang=this.textArea.getAttribute("data-lang"),this.readOnly=this.textArea.readOnly,this.isLoading=!1,this.loadFailed=!1,this.retries=0;let h=this.textArea.clientHeight,content_lines=this.textArea.value.split("\n").length,rows=this.textArea.rows;content_lines>rows&&(rows=Math.min(content_lines,50)),h=Math.max(h,19*rows,50),this.textArea.style.height=h+"px",this.wrapperNode=document.createElement("div"),this.wrapperNode.id="".concat(this.taId,"_wrapper"),this.wrapperNode.classList.add("ui_wrapper","position-relative"),this.wrapperNode.uniqueId=this.uniqueId,this.wrapperNode.style.display="none",this.wrapperNode.style.resize="vertical",this.wrapperNode.style.overflow="hidden",this.wrapperNode.style.minHeight=h+"px",this.wrapperNode.style.width="100%",this.wrapperNode.style.border="1px solid darkgrey",this.textArea.insertAdjacentElement("afterend",this.wrapperNode),this.wLast=0,this.hLast=0,this.textArea.current_ui_wrapper=this,this.uiInstance=null,this.loadUi(uiname,this.uiParams),compare_with_last_checked(textareaId);new ResizeObserver((function(){t.checkForResize()})).observe(this.wrapperNode),window.addEventListener("resize",(function(){t.checkForResize()}));const form=this.textArea.closest("form");form&&form.addEventListener("submit",(function(){null!==t.uiInstance&&(t.uiInstance.sync(),compare_with_last_checked(t.textareaId))})),document.body.addEventListener("keydown",(function keyDown(e){if("m"===e.key&&e.ctrlKey&&e.altKey){const wrapper=document.getElementById("".concat(t.taId,"_wrapper"));wrapper&&wrapper.uniqueId===t.uniqueId?null!==t.uiInstance||t.loadFailed?t.stop():t.restart():document.removeEventListener("keydown",keyDown)}}))}return InterfaceWrapper.prototype.setAllowFullScreen=function(enableFullScreen){this.isFullScreenEnable=enableFullScreen},InterfaceWrapper.prototype.loadUi=function(uiname,params){const t=this;function setLoadFailMessage(langString,errorDiv){require(["core/str"],(function(str){const s=str.get_string(langString,"qtype_coderunner"),fallback=str.get_string("ui_fallback","qtype_coderunner");Promise.all([s,fallback]).then((function(results){const s=results[0],fallback=results[1];errorDiv.innerHTML=s+"<br>"+fallback}))}))}function syncIntervalSecsBase(){return params.hasOwnProperty("sync_interval_secs")?parseInt(params.sync_interval_secs):t.DEFAULT_SYNC_INTERVAL_SECS}if(this.isLoading)return this.retries+=1,void(this.retries>20?(alert("Failed to load "+uiname+" UI component. If this error persists, please report it to the forum on coderunner.org.nz"),this.retries=0,this.loading=0):setTimeout((function(){t.loadUi(uiname,params)}),200));this.retries=0,this.params=params,this.stop(),this.uiname=uiname,""===this.uiname||"none"===this.uiname||sessionStorage.getItem("disableUis")?this.uiInstance=null:(this.isLoading=!0,require(["qtype_coderunner/ui_"+this.uiname],(function(ui){const h=t.textArea.clientHeight-t.GUTTER,w=t.textArea.clientWidth,uiInstance=new ui.Constructor(t.taId,w,h,params);if(uiInstance.failed()){t.loadFailed=!0,t.wrapperNode.style.display="none",t.textArea.style.display="",uiInstance.destroy(),t.uiInstance=null,t.textArea.classList.add("uiloadfailed");const loadFailDiv=document.createElement("div");loadFailDiv.id=t.loadFailId,loadFailDiv.className="uiloadfailed",t.textArea.parentNode.insertBefore(loadFailDiv,t.textArea),setLoadFailMessage(uiInstance.failMessage(),loadFailDiv),t.isLoading=!1}else{("function"==typeof uiInstance.ready?uiInstance.ready():Promise.resolve()).then((function(){var _uiInstance$allowFull;t.textArea.style.display="none",t.wrapperNode.style.display="";let elementToAdd=uiInstance.getElement();elementToAdd&&elementToAdd.jquery&&(elementToAdd=elementToAdd[0]),t.wrapperNode.appendChild(elementToAdd);elementToAdd.querySelectorAll("script").forEach((oldScript=>{const newScript=document.createElement("script");oldScript.src?newScript.src=oldScript.src:newScript.textContent=oldScript.textContent,document.head.appendChild(newScript),document.head.removeChild(newScript)})),"function"==typeof uiInstance.postInsert&&uiInstance.postInsert(t.wrapperNode),t.uiInstance=uiInstance,t.loadFailed=!1,t.checkForResize(),(null!==t.isFullScreenEnable?t.isFullScreenEnable:null===(_uiInstance$allowFull=uiInstance.allowFullScreen)||void 0===_uiInstance$allowFull?void 0:_uiInstance$allowFull.call(uiInstance))?t.initFullScreenToggle(t.taId):t.removeFullScreenButton(t.taId);let uiInstancePrototype=Object.getPrototypeOf(uiInstance);uiInstancePrototype.syncIntervalSecs=uiInstancePrototype.syncIntervalSecs||syncIntervalSecsBase,t.startSyncTimer(uiInstance),t.startSyncTimerForAnswerWrapper(t.textareaId)})).catch((function(){t.loadFailed=!0,t.wrapperNode.style.display="none",t.textArea.style.display="",uiInstance.destroy(),t.uiInstance=null,t.textArea.classList.add("uiloadfailed");const loadFailDiv=document.createElement("div");loadFailDiv.id=t.loadFailId,loadFailDiv.className="uiloadfailed",t.textArea.parentNode.insertBefore(loadFailDiv,t.textArea),setLoadFailMessage(uiInstance.failMessage(),loadFailDiv)})).finally((function(){t.isLoading=!1}))}})))},InterfaceWrapper.prototype.removeFullScreenButton=function(fieldId){const screenModeButton=document.getElementById("".concat(fieldId,"_wrapper")).parentNode.querySelector(".screen-mode-button");screenModeButton&&screenModeButton.remove()},InterfaceWrapper.prototype.initFullScreenToggle=function(fieldId){const wrapperEditor=document.getElementById("".concat(fieldId,"_wrapper"));function enterFullscreen(fullscreenButton,exitFullscreenButton,e){let t=this;e.preventDefault(),t.wrapperHeight=t.wrapperNode.clientHeight,t.heightEditNode=t.hLast,t.widthEditNode=t.wLast,fullscreenButton.classList.add("d-none"),wrapperEditor.append(exitFullscreenButton),wrapperEditor.addEventListener("fullscreenchange",(()=>{null===document.fullscreenElement?(t.uiInstance.resize(t.widthEditNode,t.heightEditNode),wrapperEditor.style.height=t.wrapperHeight+"px",exitFullscreenButton.classList.add("d-none"),fullscreenButton.classList.remove("d-none")):exitFullscreenButton.classList.remove("d-none")})),wrapperEditor.requestFullscreen().catch(Notification.exception)}function exitFullscreen(e){let t=this;e.preventDefault(),document.exitFullscreen(),wrapperEditor.style.height=t.wrapperHeight+"px",t.uiInstance.resize(t.widthEditNode,t.heightEditNode)}wrapperEditor.parentNode.querySelector(".screen-mode-button")||Templates.renderForPromise("qtype_coderunner/screenmode_button",{}).then((_ref=>{let{html:html}=_ref;const screenModeButton=Templates.appendNodeContents(wrapperEditor,html,"")[0],fullscreenButton=screenModeButton.querySelector(".button-fullscreen"),exitFullscreenButton=screenModeButton.querySelector(".button-exit-fullscreen");fullscreenButton.classList.remove("d-none"),fullscreenButton.addEventListener("click",enterFullscreen.bind(this,fullscreenButton,exitFullscreenButton)),exitFullscreenButton.addEventListener("click",exitFullscreen.bind(this))}))},InterfaceWrapper.prototype.startSyncTimerForAnswerWrapper=function(textareaId){isAnAnswer(textareaId)&&(this.timer=setInterval((function(){compare_with_last_checked(textareaId)}),250))},InterfaceWrapper.prototype.startSyncTimer=function(uiInstance){const timeout=uiInstance.syncIntervalSecs();timeout?this.uiInstance.timer=setInterval((function(){uiInstance.sync()}),1e3*timeout):this.uiInstance.time=null},InterfaceWrapper.prototype.stopSyncTimer=function(uiInstance){uiInstance.timer&&clearTimeout(uiInstance.timer)},InterfaceWrapper.prototype.stop=function(){null!==this.uiInstance&&(this.stopSyncTimer(this.uiInstance),this.textArea.style.display="",this.uiInstance.hasFocus()&&(this.textArea.focus(),this.textArea.selectionStart=this.textArea.value.length),this.uiInstance.destroy(),this.uiInstance=null,this.wrapperNode.style.display="none"),this.loadFailed=!1,this.textArea.classList.remove("uiloadfailed");const elementToRemove=document.getElementById(this.loadFailId);elementToRemove&&elementToRemove.parentNode.removeChild(elementToRemove)},InterfaceWrapper.prototype.restart=function(){null===this.uiInstance&&this.loadUi(this.uiname,this.params)},InterfaceWrapper.prototype.checkForResize=function(){if(this.uiInstance){const h=this.wrapperNode.clientHeight,w=this.wrapperNode.clientWidth,maxWidth=this.wrapperNode.clientWidth,hAdjusted=h-this.GUTTER,wAdjusted=Math.min(maxWidth,w);hAdjusted==this.hLast&&wAdjusted==this.wLast||(this.uiInstance.resize(wAdjusted,hAdjusted),this.hLast=hAdjusted,this.wLast=wAdjusted)}},{newUiWrapper:function(uiname,textareaId){return uiname?new InterfaceWrapper(uiname,textareaId):null},InterfaceWrapper:InterfaceWrapper}}));

//# sourceMappingURL=userinterfacewrapper.min.js.map